function verify(id) {
  let result;
  if (id === 1) {
    result = 'Doctor';
  } else {
    result = 'NOT Doctor';
  }
  return result;
}

console.log(result);
//console.log(testNum(-5));
// expected output: "NOT positive"
